language: node_js
node_js:
  - "node"

branches:
  only:
  - master

before_script:
  - npm install -g @angular/cli

script:
  - ng build --prod
  - ls -la

deploy:
  provider: surge
  project: ./static/
  domain: rav.surge.sh 